export const metadata = {
    title: "Datenschutz | GIBSONVISUALSFOTOGRAFIE",
    robots: {
        index: false,
        follow: false,
    },
};

export default function DatenschutzLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
