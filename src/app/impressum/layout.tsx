export const metadata = {
    title: "Impressum | GIBSONVISUALSFOTOGRAFIE",
    robots: {
        index: false,
        follow: false,
    },
};

export default function ImpressumLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
